import 'package:flutter/material.dart';

class AddMemoryLayout extends StatelessWidget {
  const AddMemoryLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('data'),);
  }
}
